## Product AutoGPT

#### Open AI Key
```
cp .env.example .env
```
After creating a `.env` file, enter your OpenAI key as the value for `OPENAI_API_KEY`.

#### Local Development
```
streamlit run app.py
```